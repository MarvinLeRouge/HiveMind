{
  "name": "@hivemind/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint src",
    "test": "echo 'Frontend not yet scaffolded — see BLOCK-15'",
    "test:coverage": "echo 'Frontend not yet scaffolded — see BLOCK-15'"
  },
  "dependencies": {
    "@hivemind/shared": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
