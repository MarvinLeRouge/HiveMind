name: E2E

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ── Placeholder — full suite implemented in BLOCK-22 ─────────────────────
      # Docker stack setup, migrations, seed and Playwright steps will be added
      # when the E2E suite is ready. The job must pass to unblock the CD pipeline.
      - name: Run Playwright tests
        run: echo "E2E placeholder — suite coming in BLOCK-22 (V3)"
